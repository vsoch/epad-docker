version: '2'

# UNDER DEVELOPMENT

services:
    mysql:
        image:  rubinlab/mysql
        container_name: mysql
        environment: 
          - "MYSQL_ROOT_PASSWORD=epad"
        volumes:
        - dicomproxy
        #- dicomproxy/mysql/:/var/lib/mysql
        #entrypoint:
        #- test `mysql -uroot -pepad -e "show databases like 'epaddb'" | wc -l` -gt 1 && sh /entrypoint.sh || sh /home/install.sh
       
    dcm4chee:
        image: rubinlab/dcm4chee
        container_name: dcm4chee
        ports:
        - "9080:9080"
        - "11112:11112"
        volumes:
        - dicomproxy
        #- dicomproxy/dcm4chee/:/app/dcm4chee-2.17.1-mysql/server/default/archive/
        links:
          - mysql
        depends_on:
          - mysql

    exist:
        image: rubinlab/exist
        container_name: exist
        ports:
        - "8899:8899"
        depends_on:
          - mysql

    epad:
        image: srcc/epad-docker
        container_name: epad_web
        links:
        - dcm4chee
        - mysql:mysql_host
        - exist
        environment:
        - "DOCKER_HOST=`hostname`"
        ports:
        - "8080:8080"
        volumes:
        - dicomproxy:/root/DicomProxy/

volumes:
    dicomproxy:
